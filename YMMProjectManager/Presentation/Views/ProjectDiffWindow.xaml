<Window
    x:Class="YMMProjectManager.Presentation.Views.ProjectDiffWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{Binding Title}"
    Width="1100"
    Height="700"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,8" Text="{Binding Title}" FontWeight="SemiBold" />

        <DataGrid Grid.Row="1" ItemsSource="{Binding YmmDiffEntries}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="80" />
                <DataGridTextColumn Header="Field" Binding="{Binding Field}" Width="90" />
                <DataGridTextColumn Header="Scope" Binding="{Binding Scope}" Width="*" />
                <DataGridTextColumn Header="Before" Binding="{Binding Before}" Width="2*" />
                <DataGridTextColumn Header="After" Binding="{Binding After}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <DataGrid Grid.Row="2" Margin="0,8,0,0" ItemsSource="{Binding JsonDiffEntries}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="80" />
                <DataGridTextColumn Header="Path" Binding="{Binding Scope}" Width="*" />
                <DataGridTextColumn Header="Before" Binding="{Binding Before}" Width="2*" />
                <DataGridTextColumn Header="After" Binding="{Binding After}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
